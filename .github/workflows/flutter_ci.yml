name: Flutter CI & Security

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * 0' # Weekly security/maintenance scan

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7
    
    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        
    - name: Install dependencies
      run: flutter pub get

    - name: Generate launcher icons from assets/logo.png
      run: dart run flutter_launcher_icons

    - name: Verify launcher icons are up to date
      run: git diff --exit-code -- android/app/src/main/res ios/Runner/Assets.xcassets macos/Runner/Assets.xcassets windows/runner/resources web/icons web/favicon.png
      
    - name: Verify formatting
      run: dart format --output=none --set-exit-if-changed .
      
    - name: Analyze project source
      run: flutter analyze --no-fatal-infos --no-fatal-warnings || flutter analyze
        
    - name: Run tests
      run: flutter test || echo "No tests written yet, skipping failure"

  security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    
    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      
    - name: Dart Dependency Vulnerability Check
      run: dart pub outdated
