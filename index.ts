/**
 * Legacy entry shim kept for expo prebuild tooling that inspects `main`.
 * With expo-router, the real entry is `expo-router/entry` (see package.json).
 * The playback service is registered in `app/_layout.tsx` before the tree mounts.
 */